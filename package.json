{
  "name": "tradevisionpro",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "TradeVisionPro — plateforme statique de formation financière générée depuis des documents Word.",
  "scripts": {
    "import": "python scripts/import_all.py",
    "build": "node scripts/build.mjs",
    "build:github": "node scripts/build.mjs --base-path=/TradeVisionPro/",
    "refresh": "python scripts/import_all.py && node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "test": "python -m unittest discover -s tests -p \"test_*.py\" && node --test tests/site.test.mjs"
  },
  "engines": {
    "node": ">=20",
    "python": ">=3.10"
  }
}
